[common]
pull_timeout = 5000

[gatherers]
    [gatherers.weather]
    enabled = true
    options = { api_key = "", location = "" }

[exposers]
    [exposers.prometheus]
    enabled = true
    options = { port = 3000 }
